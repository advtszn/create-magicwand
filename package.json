{
  "name": "create-magicwand-workspace",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter create-magicwand build",
    "build:cli": "bun run --filter create-magicwand build",
    "check": "bunx @biomejs/biome check .",
    "check:write": "bunx @biomejs/biome check --write .",
    "dev:cli": "bun run --filter create-magicwand dev"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.10",
    "@types/node": "^25.5.2",
    "lefthook": "^2.1.5",
    "typescript": "^5.9.3"
  }
}
